---
const posts = [
  {
    slug:     'por-que-tu-negocio-no-aparece-en-google',
    category: 'SEO',
    title:    '¿Por qué tu negocio no aparece en Google?',
    excerpt:  'La respuesta no es "hacé SEO". Es más sencillo y más profundo que eso. Acá te explico por dónde empezar sin gastar un colón.',
    readTime: '5 min',
    color:    'bg-amber-50',
    accent:   'bg-amber-500',
  },
  {
    slug:     'whatsapp-business-lo-basico',
    category: 'Digitalización',
    title:    'WhatsApp Business: lo básico que todo negocio necesita',
    excerpt:  'Catálogo de productos, respuestas automáticas y un perfil profesional. Todo gratis. Te enseño a configurarlo en 20 minutos.',
    readTime: '4 min',
    color:    'bg-green-50',
    accent:   'bg-green-600',
  },
  {
    slug:     'landing-page-vs-sitio-web',
    category: 'Web',
    title:    'Landing page vs sitio web: ¿cuál necesitás primero?',
    excerpt:  'Depende de en qué etapa está tu negocio. Te ayudo a entender la diferencia con ejemplos reales de Costa Rica.',
    readTime: '6 min',
    color:    'bg-orange-50',
    accent:   'bg-kape-naranja',
  },
];
---
<section id="blog" class="py-24 bg-kape-beige">
  <div class="max-w-6xl mx-auto px-6 lg:px-8">

    <!-- Header -->
    <div class="flex flex-col lg:flex-row lg:items-end justify-between gap-6 mb-14">
      <div>
        <div class="reveal inline-flex items-center gap-2 bg-kape-white text-kape-medio text-xs font-medium px-4 py-1.5 rounded-full mb-4">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
            <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/>
            <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
          </svg>
          Notas de tueste
        </div>
        <h2 class="reveal reveal-delay-1 font-jakarta font-bold text-3xl lg:text-4xl text-kape-oscuro">
          Artículos para digitalizar tu negocio
        </h2>
      </div>
      <a href="/blog"
         class="reveal reveal-delay-2 inline-flex items-center gap-2 text-kape-naranja font-semibold text-sm hover:underline whitespace-nowrap">
        Ver todas las notas
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>
        </svg>
      </a>
    </div>

    <!-- Posts -->
    <div class="grid md:grid-cols-3 gap-6">
      {posts.map((post, i) => (
        <article class={`reveal reveal-delay-${i + 1} ${post.color} rounded-2xl overflow-hidden
                         hover:shadow-kape-md transition-all duration-300 hover:-translate-y-1 group`}>
          <!-- Color bar top -->
          <div class={`h-1.5 ${post.accent}`}></div>
          <div class="p-6">
            <!-- Category + read time -->
            <div class="flex items-center justify-between mb-4">
              <span class="text-xs font-semibold text-kape-medio/70 uppercase tracking-wider">
                {post.category}
              </span>
              <span class="text-xs text-kape-medio/50 flex items-center gap-1">
                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
                  <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
                </svg>
                {post.readTime} de lectura
              </span>
            </div>

            <!-- Title -->
            <h3 class="font-jakarta font-bold text-kape-oscuro text-lg leading-snug mb-3 group-hover:text-kape-medio transition-colors">
              {post.title}
            </h3>

            <!-- Excerpt -->
            <p class="text-kape-medio/70 text-sm leading-relaxed mb-5">
              {post.excerpt}
            </p>

            <!-- Read link -->
            <a href={`/blog/${post.slug}`}
               class="inline-flex items-center gap-1.5 text-kape-naranja text-sm font-semibold hover:gap-2.5 transition-all">
              Leer nota
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>
              </svg>
            </a>
          </div>
        </article>
      ))}
    </div>

  </div>
</section>
